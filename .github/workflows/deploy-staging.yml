name: Dispatch remote deployment of the Lambdulus Frontend App.

on:
  push:
    branches: [ develop ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]

    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    - run: npm ci
    # - run: npm test
    - run: npm run build --if-present
      env:
        # Stopgap: CRA4/webpack4 needs the legacy OpenSSL provider on Node 17+.
        # Remove once react-scripts is upgraded.
        NODE_OPTIONS: --openssl-legacy-provider

  deploy:
    runs-on: ubuntu-latest
    needs: [build]
    if: success()

    steps:
      - name: Event Dispatch - the deployment process
        run: |
          curl --fail --show-error -X POST https://api.github.com/repos/lambdulus/staging/dispatches \
          -H 'Accept: application/vnd.github.everest-preview+json' \
          -u ${{ secrets.ACCESS_TOKEN }} \
          --data '{"event_type": "deploy-staging", "client_payload": { "repository": "'"$GITHUB_REPOSITORY"'" }}'
